{
  "name": "atomic-variants-monorepo",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "author": "Burak Emre Görmek <burak.gormek@gmail.com>",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "nx run-many --target=build",
    "build:git": "nx run-many --target=build --exclude=next,vite-example && git add -f packages/*/dist packages/swc-plugin/*.wasm",
    "postinstall": "node -e \"\" || true",
    "prepare": "husky || true",
    "sort-package": "bunx sort-package-json 'package.json' 'packages/*/package.json'"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.2.0",
    "@commitlint/config-conventional": "^20.2.0",
    "@commitlint/types": "^20.2.0",
    "@nx/js": "^22.3.3",
    "husky": "^9.1.7",
    "nx": "^22.3.3"
  }
}
